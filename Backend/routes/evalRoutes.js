const express = require("express");
const router = express.Router();
const evalController = require("../controllers/evalController");

//router.route("/similarity").post(evalController.getSimilarityScore);

module.exports = router;
